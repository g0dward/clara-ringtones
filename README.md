# Clara ringtones — audition playground

Self-contained page for auditioning Clara's candidate ringtones. All audio is
synthesised live in the browser from embedded samples — no dependencies, works
offline. Source/build tooling lives in `~/Developer/clara-sounds` (not this repo).
